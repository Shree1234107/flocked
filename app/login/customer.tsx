import { LoginForm } from "../../components/LoginForm";

export default function CustomerLoginScreen() {
  return (
    <LoginForm
      role="guest"
      title="Student sign in"
      subtitle="Sign in to join live classes."
    />
  );
}
