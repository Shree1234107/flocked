import { LoginForm } from "../../components/LoginForm";

export default function InstructorLoginScreen() {
  return (
    <LoginForm
      role="host"
      title="Become a Teacher"
      subtitle="Sign up to teach live classes on Flocked."
    />
  );
}
