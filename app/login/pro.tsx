import { LoginForm } from "../../components/LoginForm";

export default function ProLoginScreen() {
  return (
    <LoginForm
      role="host"
      title="Teacher sign in"
      subtitle="Sign in to teach live classes."
    />
  );
}
