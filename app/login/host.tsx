import { LoginForm } from "../../components/LoginForm";

export default function HostLoginScreen() {
  return (
    <LoginForm
      role="host"
      title="Become a Teacher"
      subtitle="Sign in to create live classes for your community."
    />
  );
}
